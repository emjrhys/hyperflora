extends layouts/main

block content
  h2(style='margin: 30px;') Videos in Hyperflora
  ul(class='videos')
    each vid in videos
      if (vid.host == 'youtube')
        li
          a(href='http://www.youtube.com/watch?v=' + vid.url, target='_blank')
            h3= vid['title']
